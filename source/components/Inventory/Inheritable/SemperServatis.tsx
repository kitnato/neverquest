import { Button } from "react-bootstrap"

import { LABEL_UNKNOWN } from "@neverquest/data/general"

export function SemperServatis() {
	return (
		<Button variant="outline-dark">
			<span>{LABEL_UNKNOWN}</span>
		</Button>
	)
}
